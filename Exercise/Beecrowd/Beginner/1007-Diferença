#include <iostream>

int main()
{
  int A{}, B{}, C{}, D{};
  int DIREFENCA{};

  std::cin >> A >> B >> C >> D;
  DIFERENCA = (A * B - C * D);

  std::cout << "DIFERENCA = " << DIFERENCA << std::endl;

  return 0;
}
