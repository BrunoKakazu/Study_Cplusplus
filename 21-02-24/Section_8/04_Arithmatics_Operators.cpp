/*
	Aula 04: Arithmatics Operators
*/

/*
	+ -> Adicao
	- -> Subtracao
	* -> Multiplicacao
	/ -> Divisao
	% -> Modulo ou resto

*/

#include <iostream>

using namespace std;

/*int main()
{
	int num1{ 200 };
	int num2{ 100 };

	cout << num1 << " + " << num2 << " = " << num1 + num2 << endl;

	int result{ 0 };
	

	result = num1 - num2;
	cout << num1 << " - " << num2 << " = " << result << endl;

	result = num1 * num2;
	cout << num1 << " * " << num2 << " = " << result << endl;

	result = num1 / num2;
	cout << num1 << " / " << num2 << " = " << result << endl;


	result = num2 / num1;
	cout << num2 << " / " << num1 << " = " << result << endl;

	result = num1 % num2;
	cout << num1 << " % " << num2 << " = " << result << endl;
	
	result = num1 % num2;
	cout << num2 << " % " << num1 << " = " << result << endl;

	num1 = 10;
	num2 = 3;

	result = num1 % num2;
	cout << num1 << " % " << num2 << " = " << result << endl;

	result = num1 * 10 + num2;

	// Nesta parte ele retornara 0, pois estamos utilizando valores inteiros
	cout << 5 / 10 << endl;

	// Nesta parte ele retornara 0.5, pois estamos utilizando valores double
	cout << 5.0 / 10.0;

	cout << endl;

	// ======================================================================================================================================== //
	//																	Exercicio

	// Conversao de Euro para Dolar

	const double convert_Dolar_Euro{ 0.92 };
	double euro{ 0 };
	double dolar{ 0 };

	cout << "Bem vindo ao conversor de Dolar para o Euro" << endl;
	cout << "Digite o valor em dolar para converter: ";
	cin >> dolar;
	
	euro = dolar * convert_Dolar_Euro;

	cout << dolar << " dolar convertido para euro é " << euro << endl;



	return 0;
}*/

/*
	Note:
	Nos Estados Unidos existe a seguinte ordem para aprender as regras de prioridades na matematica, o PEMDAS.
	P -> Parenteses
	E -> Expoentes
	M -> Multiplicacao
	D -> Divisao
	A -> Adicao
	S -> Subtracao

	Quando voce for ler a expressao sempre leia do final mais a direita para a esquerda.
	Voce deve respeitar a natureza da variavel, caso isso nao aconteca o resultado sera um erro.
	Variaveis declaradas como constantes nao podem ser alterados.
*/